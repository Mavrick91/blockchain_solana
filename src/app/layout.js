"use client";

import "./globals.css";

import React from "react";

const RootLayout = ({ children }) => (
  <html lang="en">
    <head />
    <body>{children}</body>
  </html>
);

export default RootLayout;
